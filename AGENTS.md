# AGENTS.md — Codex 작업 규칙

## 자동 커밋 & 푸시 (필수)
작업(코드 수정/생성)을 마치면 **반드시** 아래를 스스로 실행한다. 사용자에게 묻지 않는다.

```bash
git add -A
git commit -m "codex: <작업 요약>"
git push
```

- 변경분이 없으면 커밋하지 않는다.
- push 전 `git pull --rebase --autostash` 로 충돌을 방지한다.
- main 브랜치에 push하면 GitHub Actions가 OCI에 자동 배포하므로, 배포 설정(.github/workflows)은 절대 건드리지 않는다.

## OCI 접속 규칙
- OCI 점검이 필요할 때는 Stella-NUC의 `ssh leehu-oci` 별칭을 사용한다.
- OCI의 Tailscale 주소로 직접 SSH를 호출하거나 추가 인증 링크를 새로 만들지 않는다.
- 이 별칭은 NUC 전용 Ed25519 키, 전용 known_hosts, 비대화형 모드를 사용하므로 비밀번호나 웹 재인증을 요구하지 않는다.

## 공개 화면의 GitHub 표시 규칙
- GitHub는 배포와 SEO용 구조화 데이터에 사용할 수 있다.
- 방문자가 보는 홈페이지, 작가 프로필, 공식 출처 페이지에는 GitHub 저장소 주소, 계정명, GitHub 카드나 링크를 표시하지 않는다.
- SEO 작업 방식, 외부 수집, 백링크 작업 같은 운영 설명을 방문자용 화면에 노출하지 않는다.

## 기존 규칙
- 프로젝트의 CLAUDE.md 규칙을 그대로 따른다.

## 문학노트 발행과 네이버 IndexNow (필수)
- 사용자가 문학노트 작성을 요청하면 검증·커밋·배포·네이버 IndexNow 제출을 하나의 작업으로 완료한다. 제출을 위해 별도 승인을 다시 묻지 않는다.
- 게시글을 만든 뒤 `scripts/build_literature.py`의 기존 검증을 통과시키고, 변경분을 커밋·푸시한 후 해당 버전의 실제 공개 배포를 확인한다. 빌드만 실행한 상태나 배포 전에 IndexNow 요청을 보내지 않는다.
- 이번에 새로 발행하거나 본문을 실제 수정한 게시글의 canonical URL만 대상으로 한다. `content/literature-index-policy.json`, 공개 RSS와 sitemap, robots/noindex를 존중한다. 검색 제외 글·페이지네이션·미배포 주소·다른 호스트는 제출하지 않는다.
- 제출 전에 공개 페이지 HTTP 200, 배포된 내용과 원본의 일치, canonical, noindex 부재, robots 허용, 해당 호스트의 공개 소유 확인 키를 확인한다. 대표 호스트는 `https://xn--hu5b23z.com`이다.
- 최신 `scripts/submit_indexnow.py`의 기존 제출 기능을 사용하여 네이버 `https://searchadvisor.naver.com/indexnow`로 전송한다. URL을 명시하여 호출하고, 기본 4개 URL이나 전체 과거 글을 매번 재전송하지 않는다. 한 요청은 최대 10,000개 URL로 제한한다.
- 중복 방지를 위해 Stella-NUC의 기존 비공개 운영 기록 `C:\Users\user\AppData\Local\LeehuSEO\indexnow-receipts.json`을 먼저 확인한다. URL, 소스 커밋, 내용 해시, 시도 시각, HTTP 응답과 접수 상태를 기존 기록을 보존하며 원자적으로 저장한다. 같은 URL·같은 내용의 200/202 접수는 다시 제출하지 않는다. 202는 키 확인 대기이며 200과 구분한다.
- 기록에 URL별 내용 해시가 없는 과거 일괄 제출은 해당 기록의 소스 커밋을 기준으로 변경 여부를 확인한다. 2026-09-24 한국시간 07:13에 소스 커밋 `a75683eb1dc6192cf1b20b6aa49ec904e7391e6f`의 문학노트 6,692개가 네이버 HTTP 200으로 접수되었다. 이 숫자는 당시 기준이며 앞으로의 글 수를 고정하지 않는다.
- 네트워크·키·배포 오류가 있으면 글을 재발행하거나 날짜를 바꾸지 않는다. 미접수 변경분과 오류를 기록하고 기존 SEO 관리 작업에서 후속 확인한다. 응답을 못 받은 전송은 결과 미확정으로 남겨 반복 전송하지 않는다. 429는 즉시 반복하지 않고 다음 기존 점검으로 넘긴다.
- 작업 결과에는 발행된 글 수와 IndexNow 접수 건수·응답을 구분한다. 접수 확인 없이 제출 완료라고 보고하거나 검색 색인·순위 상승을 보장하지 않는다. 운영 기록과 제출 설명은 공개 페이지에 넣지 않는다.
